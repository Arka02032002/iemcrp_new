class Teacher{
  String id;
  String name;
  String email;
  String stream;
  int salary;

  Teacher({required this.id,required this.name,required this.email, required this.stream, required this.salary});
}